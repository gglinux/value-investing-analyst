# 评估体系总体设计（提案）

> 状态：提案，未实施。回答一个问题：**skill 一直在迭代，怎么知道是往好还是往坏迭代？**
> 原则：不另起炉灶——backtest/ 侧已建成的机制全部沿用，只补实盘侧缺口；
> 新增规则遵守 MAINTENANCE.md（SKILL.md 只减不增、单一事实源、改码门槛）。

## 0. 三个需求的现状盘点

| 需求 | 已有资产 | 真实缺口 |
|---|---|---|
| 1. 反馈闭环三断点 | 断点②（规则版本）已解：REQ-P0-08 rules_snapshot（skill_commit + 阈值注册表 + dirty 禁止落 verdict）；断点③（协议自觉）在**回测侧**已解：密封答案 .enc + seal-check + verdict 单独 commit 时序存证 + reveal 三闸门 + lint-verdict | 断点①③在**实盘侧未解**：cases/ 的 6/12 月复盘协议零机器强制；被排除/太难放弃的公司不留痕（实盘版"只有正向样本"） |
| 2. 判断账本 | 五字段关键变量 + decision-log 双写 + check_by + 复分析 diff 模式 + 触发器承接（data-reeval-trigger） | "新财报到来先逐变量对账"不是一等公民流程——diff 模式只要求读一遍旧结论；跨案例无全局台账（cases/README 跟踪清单靠手工维护） |
| 3. 实验系统 | 断言 runner + 三条计分轨 + FP/FN 双向统计 + 基线比对 + 红灯规则 + 密封 holdout（未执行批次）+ 改码门槛（≥2 案例 + E1/E2/E3） | rules_snapshot 只覆盖回测 verdict，实盘 verdict 无版本信息；没有"某版本产出的判断事后对了多少"的跨案例汇总（含指数基线对照） |

**结论**：需求3 说的实验系统，九成已在 backtest/PROMPT.md 里立法并落地。缺的不是再建一套系统，而是把实盘侧接进同一套纪律。

## 1. 总体架构：一账本、两环、一表

- **一账本**（需求2 落地）：全部判断——包括排除/太难放弃档——统一 schema 落盘，机器可查询、可对账。
- **两环**：
  - **引擎环（回测）**：已建成。测"规则判别力"，官方密封答案做裁判。保持不动。
  - **实盘环（复盘）**：新建。测"真实判断质量"，市场做裁判。复用引擎环的机制（时序存证、入口闸门、错误清零纪律），但不设档位断言——实盘没有官方答案。
- **一表**（需求3 收口）：账本 × 事后收益 × skill_commit 的 join = 版本战绩表。迭代好坏的裁判是这张表 + 回测基线零回归，不是感觉。

## 2. 最小实施方案（四个增量，按性价比排序）

### 增量 A：decision-log.json 加 `skill_commit` 字段（一行成本）

REQ-P0-08 的实盘版。新分析落盘 decision-log 时记录产出该 verdict 的 git commit。
这是版本战绩表的前提，越早加积累越多。存量 11 案例不回填（规则与回测一致：基线不动）。

### 增量 B：全局台账 `scripts/ledger.py` + 复分析入口闸门（需求1③ + 需求2）

扫描全部 `cases/*/decision-log.json`，输出全局判断台账：

- 每个 key_variable：check_by 是否到期、状态（对/错/未决）、证伪线；
- 每个案例：档位、期望 IRR、触发价区间、证伪条件清单；
- **排除/太难放弃的公司也登记**（哪怕只有公司名 + 排除原因 + 日期一行）——解决实盘负向样本缺口：被排除的公司三年涨三倍必须能被看见，否则排雷模块永远"满分"。

**复分析入口闸门**（与 validate_data"错误清零才进下一步"同款纪律）：
发现目标公司存在 decision-log 时，必须先跑 ledger 输出"上次判断对账单"（哪些变量到期、对/错、证伪是否触发过、触发价是否触及），复盘记录写回 decision-log 的"复盘记录"节，**然后才允许进入新分析**。新财报到来"先回答哪里变了、为什么变，再决定是否重建估值"由此从自觉变成机器强制。

cases/README 的跟踪清单改为从 ledger 生成（或标注以 ledger 为准），消灭手工维护的第二事实源。

### 增量 C：复盘 runner（需求1① 实盘版）

到期时（check_by / 6/12 月）机器产出对照表：关键变量判断 vs 实际走向、证伪触发有没有被注意到、期望 IRR vs 实际年化 vs **同期指数年化**（简单基线）。

设计约束：

- **不做档位断言**。实盘没有官方答案，机器只算"判断质量原始分"，复盘结论由人写、数字由机器算——与回测 C 档（机器判定、不把答案交给 AI）哲学一致。
- 统计口径直接沿用回测已立法的概念，不发明新词：
  - 回报拆"盈利驱动 vs 倍数驱动"（福耀教训——事前算术对、错在倍数不计为系统错误）；
  - 拒绝类判断用三分法：判断错误 / 判断正确+触发器有效 / 判断正确+触发器失效（茅台/福耀先例）。
- **实施时机**：等第一个真实 check_by 到期（2026-11-15 招行三个变量到期）再实现——有真实数据可校准，避免闭门造 runner。

### 增量 D：版本战绩表（需求3 收口）

ledger × 事后收益 × skill_commit 的 join：每个规则版本一组数——判断数、分档位命中率、平均 |期望 IRR − 实际年化|、相对指数基线的差。

**读表纪律**（防自欺，继承回测教训）：

- n < 5 个判断的版本不下任何结论；
- 档位对错用三分法/触发区检验，不用"涨了就是对"；
- "新版更好"的结论必须同时附：回测基线零回归 + 假阳性轨零新增（红灯规则）——实盘战绩只作为立案输入，**改判别逻辑的裁决权仍在回测环**（≥2 案例门槛 + 密封答案 + 红灯规则不可被实盘印象绕过）。

**实施时机**：ledger 积累 ≥2 个版本、≥8~10 个已复盘判断后出第一张表。

## 3. 与现有纪律的兼容性

- **SKILL.md 只减不增**：实盘复盘细则落 `references/review-loop.md`（新文件），SKILL.md 闭环机制节只加指针；ledger schema 说明写在脚本头注释。
- **单一事实源**：observations.md 仍是 skill 改进未决项的唯一台账；版本战绩表是数据输出不是路线图，不另立文件（吸取 SKILL-UPGRADE.md 被删的教训）。
- **防过度工程**：A+B 半天的量，先做；C 等 2026-11 真实到期数据；D 等积累。三个增量都设了明确的"不做什么"。

## 4. 时间线

| 时点 | 动作 |
|---|---|
| 现在起下一次分析 | 增量 A（decision-log 记 skill_commit）+ 增量 B（ledger + 入口闸门） |
| 2026-11-15（招行变量到期） | 增量 C 上线，用真实复盘校准 |
| 判断积累 ≥2 版本 ≥8 判断 | 增量 D 出第一张版本战绩表，第一次回答"迭代方向"问题 |
