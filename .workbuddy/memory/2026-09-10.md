# 2026-09-10

## F 输入口径层 + P2-8 合并实施（v2.18，续昨日的用户指令）

上午接续 2026-09-09 晚被打断的任务（中断点=修 scenarios glob 后的全量 rerun）。完成内容：

- **compute_metrics CLI 面变更**：`--market-cap` → `--market-cap-million`（单位进参数名，`allow_abbrev=False` 防 `--market-cap` 前缀静默生效）；新增 `--snapshot`（未传市值时从快照推导=单一事实源 + CLI 入口等式校验）；底稿单位非百万时引擎按 unit 字段换算；输出新增 `provenance` 块（argv/market_cap_million/snapshot——batch-3 起重跑自读，替代手工 RERUN_PARAMS）。
- **等式校验三处修正（按用户指令）**：A/H 分部口径优先（`_dual_listed`：`price_a×a_million + price_h×h_million×fx`）；容差 10% 不拦 5% 日期漂移；WARN 级不硬拦；A+H 缺分部拆分跳过留痕（缺证据不裁决）；快照缺市值且未传参 → 拒绝执行（存在性前置）。
- **check_market_snapshot.py 扩展**：unit 前缀匹配（亿元/百万元）；legacy 读取链补 `price_unadjusted`、`market_cap_yi`；裸股本量级自检（>10^6 判原始股数 ÷10^6，双汇 33 亿股实证）。
- **rerun 引擎接入（P2-8 升格为第三批阻塞项后清偿）**：RERUN_PARAMS 三类键独立可选（moat/iv_growth/market_cap_million）；**scenarios.json 无后缀命名修复**——此前鞍钢/神华/NFLX 等 5 案的 check_scenarios/reverse_dcf 在 rerun 被静默跳过（glob `scenarios_*.json` 匹配不上），GATE 码全部误判不可复现；软银故意不设 moat/iv（ADJ2 人工裁决案，反推会产出 GATE2_UNRATED 假漂移）。
- **known_failures 登记更新**：ZM 漂移全清除 → engine_drift 登记删除（runner「登记过期」提示消失）；其余 5 案（双汇/鞍钢/神华/软银/NFLX）note 更新为精确残差。残差三类构成：①mos_calc 派生判定（GATE1_FAIL）永不在 rerun 路径；②trigger_reachability/check_market_snapshot 不在 rerun 路径；③今日引擎新增码（S9/S5/GATE_EFFECTIVE_HURDLE_GAP）对冻结 verdict 是「新增」——E 阶段豁免的设计内效果（NFLX 三 M_ 码消失）。
- **测试**：12b 段 8 条守护（单位换算/A+H 分部/容差不误报/10 倍错位/跳过留痕/快照推导/旧参数名拒绝/缺市值拒绝执行）；参数名同步 9.11 与 12 段。注意：测试预期一度写错（1000 百万 = 10 亿不是 100000），引擎是对的。
- **文档**：SKILL-UPGRADE.md F 节标记完成 + 输入层残差清单（reverse_dcf implied-growth 的 --market-cap 仍无单位语义=第二例后再改等三条）；SKILL.md/metric-playbook.md 参数名更新。
- 验收：tests 全绿；`--rerun --baseline` 12 案与基线一致；exit=0。

**设计要点（第一性原理）**：等式校验的使命是拦 10 倍量纲错位不是拦日期漂移——容差与告警级别的选择由「假阳性成本 vs 假阴性成本」不对称决定；单位进参数名消灭的是「隐式单位契约」，隐式契约靠人记忆维护必然在某次手抄时断裂（双汇实证）。
