# 维护纪律（修改本仓库任何文件前必读）

> 本文件是**改 skill 的人（人类或 AI Agent）的操作规程**。分析运行时不加载本文件；
> SKILL.md 只保留一行指针到这里。膨胀红线由 `tests/run_tests.py` 机器强制执行——
> 超线即测试失败，不依赖自觉。

## 1. 膨胀红线（tests 机器强制）

- **SKILL.md 只减不增**：新增方法论、市场细节、踩坑记录一律写入 `references/` 对应文件，SKILL.md 最多留一行指针。主文件每次加载都吃上下文，膨胀会稀释 30 秒纪律卡的执行力——skill 的敌人已从"缺纪律"变成"纪律太多被稀释"。
- **写作纪律（SKILL.md 与 references/ 通用）**：正文侧重**结论、步骤、判据要点**，每条规则最多带一句违反后果或一个锚例；起因叙述、背景故事、逐案统计分布、版本号标记一律不进正文——它们的归宿是 `backtest/BATCH*_FINDINGS.md`（批次归档，不可变）或 `backtest/observations.md`（未决项台账）。判别标准：删掉这段后，执行者做错事时会不会少了发现途径——会，则它是规则要留；不会，则它是过程史要搬。
- **同一条规则只允许一处事实源**：操作细则单属于对应 references 文件，SKILL.md 只放指针；估值方法细节单属于 valuation-guide 方法树，类型卡只放路由语义。两处活文档写同一细节 = 漂移源（券商缺行就是这么来的）。
- **体积守卫**：`tests/run_tests.py` 对 SKILL.md 与 `references/*.md` 总量设硬上限，超线即 FAIL。**红线是意识闸，不是配额**——目的是防无意识膨胀（每轮加一点、从不压缩），不是逼人为过线而切有价值的内容；超过红线时做一次显式判断：内容确有判断价值则说明理由、先在别处压缩等量、再上调红线数字；只是过程性叙述则搬去批次归档。

## 2. 改动与测试

- **改 `scripts/` 下任何脚本，改动前后必须各跑一次 `python3 tests/run_tests.py`**（覆盖：正常/周期高位/周期低位/亏损/金融股门控/capex拆分/估值引擎/股息口径/银行管道/报告校验负向）。测试不过禁止提交——上次正常化和 capex 拆分能安全落地，靠的就是这道闸。
- 新增计算逻辑时同步在 tests 加对应场景，**负向用例（该拒绝的必须拒绝、该逮住的必须逮住）优先于正向用例**。
- **钩子已落地**，克隆仓库后执行一次 `bash scripts/install-hooks.sh`（`.git/hooks` 不受版本控制，故钩子源码存放在 `scripts/hooks/` 并软链过去，这样钩子本身也能被 review 和迭代）。三道闸：① 测试套件全绿；② 提交信息乱码/噪声检测（历史 `61666b1` 曾混入"叔叔""香肠"等生成噪声，本仓库以公开身份对外发布，提交信息属对外内容）；③ 暂存区隐私与临时文件扫描（个人路径、内部邮箱、持仓/加仓价位、`audit_*.py` 等调试脚本）。紧急绕过：`git commit --no-verify`。
- **回测批次相关规则**（隔离证据、批次收官闭环）见 `backtest/PROMPT.md`；告警码注册表见 `backtest/alert-registry.md`（事实源 `scripts/alert_codes.py`）。

## 3. 数值引擎的体检纪律

- **量纲/口径类 bug 用"极端值反推"体检**：数值引擎最危险的错误不是算错，而是**公式的隐含假设与现实不符**——这类错误在任何单个案例里都看起来"合理"，只有用边界条件反推才会暴露。标准动作：给引擎喂一个答案已知的极端输入，看它是否吐出理论值。例：`expected-return` 喂"现价 = 内在价值"，正确答案必须是"期望 IRR = 折现率"；旧引擎吐出 0%，一步就暴露了"内在价值 5 年原地不动"的隐含假设，而该错误已让 4 个案例结论出错、且此前 10 个案例逐一评审都没发现。
- **闸门/门槛类参数必须做"判别力检验"**：新增或修改任何阈值后，把历史案例全部重算一遍看分布。若结果**过度集中在阈值附近**（如 9 个案例 IRR 全落在 0~8%），或**几乎全过/全不过**，说明阈值失去判别力或口径有系统性偏差——此时先怀疑公式与阈值校准，而不是断言"市场就是这么贵"。同时检验新增闸门与既有闸门是否**数学上相互推导**（如期望 IRR 与安全边际在期望值口径下互为单调函数），互为推导的两道闸门等于同一道闸门算两遍，必须换维度（如从期望值换到下行分布）才构成真正的独立约束。
